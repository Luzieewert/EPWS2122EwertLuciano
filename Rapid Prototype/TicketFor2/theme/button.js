import {StyleSheet} from "react-native";

const button = StyleSheet.create({
base: {
    backgroundColor: '#000',
    elevation: 5,
    padding: 5,
    alignItems: "center",
}
});

export default button